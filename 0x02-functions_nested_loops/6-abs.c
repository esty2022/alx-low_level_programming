#include "main.h"

/**
*_abs - computes the absolute value
*@d: the integer
*Return: The absolute value*
*/
int _abs(int d)
{
	if (d < 0)
	{
		int abs_val;

		abs_val = d * -1;

		return (abs_val);
	}

		return (0);
		return (d);
}
